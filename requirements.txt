# Core dependencies for bipas6.py
# Install with: pip install -r requirements.txt

# BEST HTTP client for anti-detection (RECOMMENDED)
# curl_cffi impersonates real Chrome TLS fingerprint (JA3)
curl_cffi>=0.5.0

# Fallback HTTP clients
requests>=2.28.0
aiohttp>=3.8.0
urllib3>=1.26.0

# Alternative TLS client (optional)
# tls-client>=0.2.0

# Data handling and parsing
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Faker for generating realistic data
faker>=18.0.0

# Scientific computing (used for statistical analysis)
numpy>=1.23.0

# Terminal colors
colorama>=0.4.6

# Optional: for improved datetime parsing
python-dateutil>=2.8.0

# Optional: for brotli decompression support
brotli>=1.0.9

# IP scanning and validation (optional but recommended)
# Requires nmap binary installed on system: apt install nmap
python-nmap>=0.7.1

# DNS lookup
dnspython>=2.4.0
